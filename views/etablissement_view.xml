<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- action -->
    <record id="etablissement_action" model="ir.actions.act_window">
        <field name="name">etablissement</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">etablissement</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                create your first etablissement
            </p>
        </field>
    </record>

    <menuitem
        id="menu_etablissement"
        name="etablissement"
        action="etablissement_action"
        parent="menu_other_root"
        sequence="10"/>

</odoo>
